import React, { Component } from 'react';
import Hero from '../Hero';

export default class App extends Component {
  render () {
    return (
      <div id="wrapper">
        <Hero />
      </div>
    );
  }
}
